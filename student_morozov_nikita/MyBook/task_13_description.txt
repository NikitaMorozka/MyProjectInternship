**Контекст задачи (краткое описание) **
Написать тест, который доказывает, что код заполнения
              ответа написанный в предыдущем задании работает правильно
**Что было сделано и как? (краткое описание)**
Я написал код теста. Необходимо было изучить доп. материал, изучал по роликам dmdev
**Зачем это было сделано? Чему научился в процессе выполнения задания?**
изучил основы написания тестов. Появилось понимание для чего необходима аннотация @BeforeAll и @AfterAll

**Сколько потратил времени на выполнение задачи?**
Я потратил порядка 2 часов на то, чтобы изучить и потренироваться. Много времени было потрачено, на то, чтобы собраться
для изучения тестов, думал будет сложнее. Буду работать над собой

**Заметки, идеи, предложения в свободной форме**
 // Проверяет, равны ли ожидаемое и фактическое значения
        assertEquals(10, 5 + 5);
        assertNotEquals(5, 2 + 2); // Проверяет, что значения не равны
        assertTrue(3 < 5); // Проверяет, что выражение возвращает true
        assertFalse(3 > 5);// Проверяет, что выражение возвращает false
        assertNull(null);  // Проверяет, что объект равен null
        assertNotNull(new Object());  // Проверяет, что объект не равен null
        assertArrayEquals(new int[]{1, 2, 3}, new int[]{1, 2, 3});// Проверяет, что два массива равны

        Object obj = new Object();
        assertSame(obj, obj); // Проверяет, что два объекта имеют одинаковые ссылки
        assertNotSame(new Object(), new Object());  // Проверяет, что два объекта имеют разные ссылки